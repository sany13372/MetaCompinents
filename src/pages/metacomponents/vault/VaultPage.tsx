import React from 'react';
import Vault from "../../../features/metacomponents/vault";

const VaultPage = () => {
    return (
        <main>
            <Vault />
        </main>
    );
};

export default VaultPage;