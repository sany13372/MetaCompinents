import React from 'react';
import Staging from "../../../features/metacomponents/staging";

const StagingPage = () => {
    return (
        <main>
            <Staging />
        </main>
    );
};

export default StagingPage;