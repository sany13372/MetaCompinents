import React from 'react';
import NotFound from "../../features/static/notFound";

const NotFoundPage = () => {
    return (
        <main>
            <NotFound />
        </main>
    );
};

export default NotFoundPage;